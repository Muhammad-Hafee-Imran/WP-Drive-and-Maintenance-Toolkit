const Header = () => (
    <header className="wpmudev-pm-header">
        <h1>Posts Maintenance</h1>
        <p>Run a scan across your posts to detect and update metadata.</p>
    </header>
);

export default Header;
